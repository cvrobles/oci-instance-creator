name: OCI Free Tier Bot Runner
on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  run-creator:
    runs-on: ubuntu-latest
    steps:
      - name: Install Official Oracle CLI
        run: |
          curl -sL https://githubusercontent.com > /dev/null
          bash -c "$(curl -L https://githubusercontent.com)" -- --accept-all-defaults
          echo "/home/runner/bin" >> $GITHUB_PATH

      - name: Initialize OCI Credentials
        run: |
          mkdir -p ~/.oci
          echo "${{ secrets.OCI_KEY_FILE }}" > ~/.oci/oci_api_key.pem
          chmod 600 ~/.oci/oci_api_key.pem
          
          cat << EOF > ~/.oci/config
          [DEFAULT]
          user=${{ secrets.OCI_USER_OCID }}
          fingerprint=${{ secrets.OCI_FINGERPRINT }}
          tenancy=${{ secrets.OCI_TENANCY_OCID }}
          region=${{ secrets.OCI_REGION }}
          key_file=~/.oci/oci_api_key.pem
          EOF
          chmod 600 ~/.oci/config

      - name: Execute Launch Command
        run: |
          AD_NAME=$(oci iam availability-domain list --query "data[0].name" --raw-output)
          
          oci compute instance launch \
            --compartment-id "${{ secrets.OCI_COMPARTMENT_OCID }}" \
            --availability-domain "$AD_NAME" \
            --shape "VM.Standard.A1.Flex" \
            --shape-config '{"ocpus":2,"memoryInGBs":12}' \
            --subnet-id "${{ secrets.OCI_SUBNET_OCID }}" \
            --image-id "${{ secrets.OCI_IMAGE_OCID }}" \
            --display-name "AlwaysFreeUbuntu" \
            --ssh-authorized-keys "${{ secrets.SSH_PUBLIC_KEY }}" \
            --assign-public-ip true \
            --boot-volume-size-in-gbs 50
